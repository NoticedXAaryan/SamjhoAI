# `_legacy/`

Deprecated code kept temporarily for reference during migrations.

- Do **not** import anything from this folder.
- New code must live under `src/features/` and `src/shared/`.
- Once Sprint 7 QA sign-off is complete, this folder can be deleted.

